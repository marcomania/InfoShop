services:
  # Servicio de PHP (Aplicación)
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: aynishop-app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
    networks:
      - aynishop-network

  # Servidor Web Nginx
  nginx:
    image: nginx:1.25-alpine
    container_name: aynishop-nginx
    restart: unless-stopped
    ports:
      - "8000:80"
    volumes:
      - ./:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - aynishop-network

  # Base de Datos PostgreSQL
  db:
    image: mysql:8
    container_name: aynishop-db
    restart: unless-stopped
    ports:
      - "3306:3306"
    volumes:
      - aynishop-db-data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-rootpassword}
      MYSQL_DATABASE: ${DB_DATABASE:-laravel}
    networks:
      - aynishop-network

  # Capturador de Email
  mailpit:
    image: axllent/mailpit:latest
    container_name: aynishop-mailpit
    restart: unless-stopped
    ports:
      - "1025:1025" # Puerto SMTP
      - "8025:8025" # Interfaz Web

networks:
  aynishop-network:
    driver: bridge

volumes:
  aynishop-db-data:
    driver: local
